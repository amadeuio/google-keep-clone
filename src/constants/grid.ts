export interface NoteGridConfig {
  noteWidth: number;
  noteHeight: number;
  gap: number;
  columns: number;
}

export const GRID_CONFIG: NoteGridConfig = {
  noteWidth: 237,
  noteHeight: 200,
  gap: 10,
  columns: 3,
};
