export interface NoteGridConfig {
  noteWidth: number;
  gap: number;
  pinnedSectionGap: number;
}

export const GRID_CONFIG: NoteGridConfig = {
  noteWidth: 238,
  gap: 16,
  pinnedSectionGap: 68,
};
